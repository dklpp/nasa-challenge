{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Typecheck Next app",
			"type": "shell",
			"command": "npm ls >/dev/null 2>&1 || npm install --silent && npx tsc -p tsconfig.json --noEmit",
			"problemMatcher": [
				"$tsc"
			],
			"group": "build"
		}
	]
}